{
  "name": "chatgpt-codex-orchestrator",
  "version": "0.1.0-alpha.1",
  "private": true,
  "type": "module",
  "description": "Reliable Brain Transport (M1) for ChatGPT-command orchestration via the Codex in-app browser.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/atomic-turn.js && node --check src/iab-transport.js && node --check src/index.js"
  },
  "engines": { "node": ">=18" }
}